name: todo_flutter
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2          # 2 SPACJE wcięcia – takie samo jak inne paczki
  flutter_dotenv: ^5.0.2
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # plik z kluczami (.env) – przenieśliśmy go do katalogu assets
  assets:
    - assets/dev.env
